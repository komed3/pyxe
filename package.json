{
  "name": "pyxe",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "npx tsc -b tsconfig.build.json",
    "test": "npm install && node ./test/index.js"
  }
}