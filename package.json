{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "npx tsc -b tsconfig.build.json",
    "test": "node ./test/index.js"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}